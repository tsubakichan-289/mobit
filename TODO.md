上半平面でのメビウス変換の軌跡を描画するためのコードを作成します，
具体的には，

L作用
L(z) -> z+1
R作用
R(z) -> z/(z+1)

をn回繰り返すことで得られる軌跡を描画します．

画面にはCayley変換で上半平面を単位円に写像したものもを描画します．

そのうえで円をクリックすると，その円に対応する複素数とその複素数に対するL作用とR作用の軌跡を描画します．

また，再帰的にL作用とR作用を繰り返すことで得られる軌跡も描画します．

何回繰り返すかはnの値を指定することで決定します．
